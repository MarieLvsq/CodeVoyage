@model CodeVoyage.Models.OffrePerso
@{
    ViewBag.Title = "Formulaire";
    Layout = "_LayoutMembre";
}
<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Ajouter une Offre de Voyage</title>
</head>
<body>
    @using (Html.BeginForm())
    {
        <fieldset>

            <table align="center" text-align="center">
                <tr>
                    <th>
                        Créez votre Offre personnalisée

                    </th>

                </tr>
                <tr>
                    <td>
                        @Html.LabelFor(m => Model.Offre)
                    </td>
                </tr>
                <tr>
             
                </tr>
                <tr>
                </tr>
                <td class="form-control">
                    @Html.DropDownListFor(m => m.OffreId,new SelectList(ViewBag.EvenementList, "Id", "EvenementDescription"))
                    @Html.ValidationMessageFor(m => Model.Offre)
                </td>
                <tr>
                    <td>
                        @Html.LabelFor(m => Model.ServicePerso)
                    </td>
                </tr>
                <tr>
                    <td class="form-control">
                        @Html.DropDownListFor(m => m.ServicePersoId, new SelectList(ViewBag.ServiceList, "Id", "ServiceDescription"))
                        @Html.ValidationMessageFor(m => Model.ServicePerso)
                    </td>
                </tr>

                @*<tr>
            <td class="form-control">
                @Html.TextBoxFor(m => Model.prixTotal)
                @Html.ValidationMessageFor(m => Model.prixTotal)
            </td>
        </tr>*@

                <tr>
                    <td>
                        <button type="submit" class="btn btn-secondary"> Créer mon offre perso</button>
                        @Html.ActionLink("Je r�serve", "CreerReservation", "Reservation")
                    </td>
                </tr>
            </table>
        </fieldset>


    }

</body>
</html>

